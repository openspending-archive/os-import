var _ = require('lodash');
var assert = require('chai').assert;
process.env.NODE_ENV = 'test';

describe('Tabular file manager', function() {
  it('validates and infers schema of CSV string', function(done) { done(); });
  it('can download CSV from URL', function(done) { done(); });

  it(
    'download only first maxSize bytes of CSV if options.maxSize passed',
    function(done) { done(); }
  );
});